// Public, non-secret configuration. For GitHub Pages, set apiBaseUrl to the
// protected deployed API origin, e.g. "https://your-api.example.com".
// It is intentionally blank in the repository so local full-stack previews
// continue to use the same origin.
window.__AGENTIC_CONFIG__ = {
  apiBaseUrl: "",
};
